import { DEFAULT_CIPHER, decrypt, encrypt } from "./binding";
import { plugin, PluginOptions } from "./plugin";

export {
  DEFAULT_CIPHER,
  decrypt,
  encrypt,
  plugin,
  PluginOptions,
  plugin as default,
};
